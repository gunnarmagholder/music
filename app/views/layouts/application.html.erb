<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN"
	"http://www.w3.org/TR/html4/strict.dtd">
<html>
<head>
	<title>Music 4 the classes</title>
	<%= stylesheet_link_tag 'music' %>
    <%= javascript_include_tag :defaults %>
</head>
<body>
	<h1>Music 4 the classes</h1>
	<div id="nav">
	  <%= link_to "Home",     :controller => "root", :action => "index" %> |
	 
	  <% if logged_in?%>  
	     <%= link_to "(y)ourMusic", :controller => "users", :action => "show", :id => current_user %> |
	  <% else %>
	  <% end %>
	  <%= link_to "Impressum", :controller => "root", :action => "about" %> |
	  <%= link_to "Help",     :controller => "root", :action => "help" %> 
	</div>
	<% if logged_in? %>
	  <p><strong>Hello <%=h current_user.firstname %></strong> <%= link_to 'Logout', logout_path %></p> 
	  <p style="color: green"><%= flash[:notice] %></p>
	<% else %>
	  <p><strong>You are currently logged out.</strong></p>	
	  <p> 
	    <%= link_to 'Login', login_path %> oder 
	    <%= link_to 'Sign_up', signup_path %>
	  </p>

	<% end %>
	<%= yield  %>

	</body>
	</html>
